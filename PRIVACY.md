# 素页 PlainLeaf 隐私政策

**生效日期：2026-09-26　适用版本：v0.4.0-beta 及以后**

素页（以下简称"本应用"）是一款**本地优先**的图文记录工具。本政策说明本应用会接触哪些数据、做什么用、以及你对它们的控制权。

一句话总结：**你的记录只存在你自己的设备上。本应用没有服务器，不收集、不上传、不分析你的任何内容。**

---

## 一、我们不收集什么

本应用：

- **没有账号体系**，不要求注册，不需要手机号或邮箱；
- **不收集任何使用数据**：没有统计 SDK、没有埋点、没有崩溃上报、没有广告 SDK；
- **没有自建服务器**，不会把你的记录、图片、标签、待办等任何内容回传到我们这里；
- **不读取设备标识**（IMEI / Android ID / 广告 ID 等一概不读取）；
- **不申请通讯录、短信、通话记录、精确定位等权限**。

---

## 二、你的数据存在哪里

| 数据 | 存放位置 |
|---|---|
| 记录正文、笔记本、标签、待办、学习记录 | 设备上**应用私有目录**内的 SQLite 数据库 |
| 图片原图、缩略图 | 设备上**应用私有目录**内的 `media/`、`thumb/`、`medium/` 目录 |
| 应用锁密码 | 只保存**校验值**（不可逆），不保存密码本身 |
| 云备份凭据、加密密钥 | 系统安全容器（Android Keystore / iOS Keychain / Windows 凭据管理器） |

"应用私有目录"指受到操作系统隔离保护的目录。在未取得 root / 越狱的普通设备上，其他应用无法读取。

---

## 三、本应用会联网吗

**只有在你主动配置"云备份（WebDAV）"之后才会联网，且仅用于你指定的那个云盘。**

- 云备份使用 **WebDAV** 协议，你可以填入任何**你自己**的云盘地址（如坚果云）；
- 上传的备份包由**你本人**的账号保存，我们无法访问，也无从得知你的云盘地址与账号；
- 备份包内的凭据**已被排除**（云盘口令不会被打进备份包，因此它不会被上传到云端）；
- 备份包可选 **AES-256-GCM 整包加密**，密码由你设定，**我们不保存、也无法找回**；
- **未配置云备份时，本应用不会发起任何网络请求。**

除上述云备份外，本应用不会与任何第三方通信。

---

## 四、权限用途

本应用只申请三项权限，每一项都对应一个明确功能：

| 权限 | 用途 | 说明 |
|---|---|---|
| `INTERNET`（网络） | 云备份上传 / 恢复 | Android 将其归为"普通权限"，安装时不弹窗。**不配置云备份则不使用** |
| `USE_BIOMETRIC`（生物识别） | 应用锁的指纹/面容快速解锁 | 生物特征数据由**操作系统**处理与保管，本应用既接收不到也存储不了 |
| `WRITE_EXTERNAL_STORAGE`（存储，仅 Android 10 及以下） | 把图片保存到系统相册 | 已用 `maxSdkVersion=29` 收窄范围；Android 11 及以上走系统相册接口，**不需要此权限** |

此外，选择图片 / 拍照使用系统自带的文件选择器与相机，**本应用不申请**相机与相册的常驻权限。

---

## 五、关于应用锁的边界（如实说明）

应用锁**保护的是"打开应用的入口"，不是数据库文件本身**：

- 开启后，冷启动或切回应用时需要密码或生物识别；
- 它能防住的是"手机被借用时被顺手翻看"这一场景；
- **它不会加密本地数据库**。具备 root 权限、或能物理提取设备数据的高级手段，仍可能读取到数据。

如果你的威胁模型高于此，请配合使用：① 云备份的加密选项（AES-256-GCM），② 设备本身的系统级加密与锁屏。

---

## 六、你的数据，你完全控制

- **导出**：可随时把全部记录导出为 Markdown、PDF，或导出为备份包（`.plbk`）；
- **删除**：删除记录采用"移入回收站 → 二次确认永久删除"的两步流程，避免误删；
- **彻底清除**：卸载应用会删除应用私有目录内的全部数据；
  ⚠️ 你上传到云盘的备份包**不会**随之删除，需要你自行到云盘清理；
- **撤回云备份**：在设置中清空 WebDAV 配置即可，此后不再联网。

---

## 七、第三方组件

本应用使用了若干开源库（Flutter、Drift、Riverpod、quill 等）。其中涉及平台能力的库及其数据行为：

| 组件 | 作用 | 数据行为 |
|---|---|---|
| `flutter_secure_storage` | 存放密钥与云盘凭据 | 数据进**系统安全容器**，不出设备 |
| `local_auth` | 调起系统生物识别 | 仅向系统请求"验证是否通过"，**拿不到**生物特征数据 |
| `gal` | 保存图片到系统相册 | 仅在你点击"保存"时写入，之后由系统相册管理 |
| `pdf` / `cryptography` | 生成 PDF、加解密 | 纯本地计算，不联网 |

这些库均为开源软件，可在项目的 `pubspec.yaml` 中查看到完整清单与版本。

---

## 八、未成年人

本应用不面向未满 14 周岁的儿童单独提供服务，也不会有意收集未成年人的个人信息（因为本应用不收集任何人的个人信息）。

---

## 九、政策变更

本政策如有更新，会随应用版本发布并更新文首的"生效日期"。涉及数据处理方式的重大变更，会在应用内以显著方式提示。

---

## 十、联系我们

对本政策或数据处理方式有疑问，可通过以下渠道联系：

- 项目仓库 Issues：<https://github.com/yufenfei111/plainleaf/issues>

---

> **开发者自查备注（不随政策对外发布，可删）**
> 本政策逐条对应代码事实，修改前请先确认：
> - "无第三方 SDK" → `pubspec.yaml` 依赖清单
> - 权限清单 → `android/app/src/main/AndroidManifest.xml`（INTERNET 那条在 W16 才补上，
>   见 `docs/release-checklist-w16.md` 第二节）
> - 凭据不入备份包 → `test/w13_webdav_test.dart` 用例 ⑪ 有断言守着
> - 加密包头部 → `lib/core/security/crypto_service.dart`（`PLSEC1` 自描述容器）
> - 应用锁只存校验值 → `lib/core/security/app_lock.dart`
