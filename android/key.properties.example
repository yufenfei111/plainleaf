# 发布签名配置模板（W16 发布准备）
#
# 用法：
#   1. 复制本文件为同目录下的 key.properties（**不要提交它**，已在 .gitignore）
#   2. 按 docs/release-checklist-w16.md 生成正式密钥
#   3. 把下面的值换成你自己的
#
# storeFile 的路径相对于 android/app/ 解析。
# 推荐把 .jks 放在 android/ 目录下（即下面的写法 ../plainleaf-release.jks），
# 也可以写绝对路径。
#
# ⚠️ 这个文件一旦丢失，已发布的 App 就无法再更新（Android 要求同一签名的包才能覆盖安装）。
# 请连同密码一起备份到密码管理器和离线介质，不要只留在本机。

storePassword=在此填写密钥库密码
keyPassword=在此填写密钥密码
keyAlias=plainleaf
storeFile=../plainleaf-release.jks
